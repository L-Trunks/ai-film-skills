---
id: <kebab-case-id>
name: <中文名>
used_by: []
use_count: 0
last_used: null
verified: 未自测
---

# <骨架名>

> 复制本文件改。`use_count: 0` 的骨架优先于已用过的 —— `fingerprint.md` 的 structure 层会检查这个数。

---

## 适用什么

哪种片型、哪种变量表组合适合走这套。写清楚**不适用**什么，比写适用更有用。

## 前置要求

这套骨架对 profile 有什么要求。例如：

```yaml
requires:
  max_shot_sec: ">= 5"     # 慢节奏骨架需要长单镜
  frame_count: ">= 120"
```

不写等于无要求。**写了就要在开片第 1 步核对。**

## 骨架

段落划分与每段镜数、时长。给出可直接套用的形式：

```
<段名> N镜 × X.Xs → <段名> N镜 × X.Xs → ...
```

或者说明「无段落划分，全片等长」。

## 镜头数公式

```
镜头数 = ...   # 用 profile 变量，不要写死常数
```

## 关键设计点

这套骨架里哪一处是不能动的？为什么？

（trailer-8seg 的答案是「急停那 1.5 秒」，ambient-flat 的答案是「同角色镜不相邻」。每套骨架都该有一两条这样的东西，没有的话说明这套骨架还没成型。）

## 调色 / 拼接注意

用哪个脚本、有没有特殊参数、翻过什么车。

## 已知的同质化陷阱

首次撰写时留空。**每用一次回来补一行** —— 记下这次在哪个位置做了和上次一样的选择。

| 位置 | 用过什么 | 建议 |
|---|---|---|
| | | |
